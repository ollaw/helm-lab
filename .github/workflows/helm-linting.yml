name: helm-linting

on: [push]

jobs:
  template-dev:
    uses: ./.github/workflows/reusable-workflow.yml
    with:
      environment: dev
  template-staging:
    uses: ./.github/workflows/reusable-workflow.yml
    with:
      environment: staging
